import styled from 'styled-components';

const Logo = styled.section`
  font-size : 32px;
  text-align : center;
  padding : 10px;
  & > a {
    text-decoration : none;
    color : #fff;
  }
`;

export default Logo;
